# Create a vector v containing 7 river data elements.
v <- c(length(rivers), sum(rivers), mean(rivers), median(rivers), var(rivers), 
    sd(rivers), min(rivers), max(rivers))

print(v) # Print the vector v