#include <iostream> // The iostream allows for input commands from external sources such
                    // as keyboards and then outputs data to a monitor

using namespace std; // namespace std is a region that contains identifiers within it
                    // examples of what's included are cout, cin, string
                    // if this isn't used it here it has to be added to any of the identifiers
                    // contained within it. For example cout would be std::cout 

//main() is where ther program will enter to begin executing the program
int main() {
    cout << "Comments"; //cout is the output of data 
    return 0;   // return 0 allows us to know that the program ran successfully without any errors
}   // This signifies the end of our main function